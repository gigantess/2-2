시도 1(15/17)
✓
AI 사전평가 결과
평가 시각: 2026-08-11 17:14:08

88%
(15 / 17 항목 통과)
종합 평가
대부분 요구사항을 코드와 문서로 충실히 구현했습니다. CLI 입력 검증, LLM 1차 스키마 강제·재시도, 장소 검색 연동, 결과 저장(원본 JSON/MD), 에러 누적·리포트 반영, 캐시 기능 등이 명확히 구현되어 있습니다. 일부 항목(정밀한 타입검증·추천도시 정규화)은 보완이 필요합니다.

항목별 평가 (17개)
PASS
평가 항목 #1
근거
travel_planner.py > '사용법: python travel_planner.py --date YYYY-MM-DD (예: 2026-03-15)\n'
잘한 점
argparse로 -date 옵션과 형식/유효성 검증을 구현함
부족한 점
사용법 출력은 오류 상황에서만 표시되며 도움말 예시 보강을 권장
보완
명시적 예시와 함께 argparse의 --help 출력 또는 에러 메시지 개선을 고려하세요
PASS
평가 항목 #2
근거
llm_client.py > ' "recommended_city": "추천 도시 이름 (예: 제주, 강릉, 경주)",'
잘한 점
프롬프트에 1차 JSON 스키마를 명확히 포함하고 필수키 검증을 수행함
부족한 점
현재는 키 존재만 검증하고 타입체크는 없음
보완
각 키의 기대 타입(예: events는 array) 체크를 문서화해 검증 로직에 추가하세요
PASS
평가 항목 #3
근거
travel_planner.py > 'places = place_client.search_restaurants(city, errors, count=5)'
잘한 점
추천도시를 받아 지도 API 호출로 연결하는 파이프라인이 구현됨
부족한 점
장소 검색 실패 시 폴백 동작은 있으나 실패 원인별 추가 로깅 개선 여지
보완
실패 케이스별(네트워크/인증/빈결과) 더 상세한 컨텍스트 로그를 추가하세요
PASS
평가 항목 #4
근거
report_generator.py > 'RESULTS_DIR = "results"'
잘한 점
results/ 디렉터리와 원본 JSON·Markdown 저장 함수가 구현되어 있음
부족한 점
파일 저장시 에러(권한/디스크) 처리 시나리오가 상세하지 않음
보완
파일 쓰기 실패 시 재시도/임시파일 사용 등 안전한 저장 전략을 문서화하세요
PASS
평가 항목 #5
근거
README.md > '**보안 관리 정책**: API 키를 소스코드, README, Public Git 저장소에 절대로 노출하지 마세요.'
잘한 점
API 키를 .env/환경변수로 관리하고 코드에 하드코딩하지 않는 지침을 명시함
부족한 점
결과물(실행 후 생성 파일)에 대한 키 스캔 자동화 언급은 없음
보완
저장 전에 결과 파일에서 잠재적 키 패턴을 스캔하는 검증 단계를 권장합니다
PASS
평가 항목 #6
근거
doc/walkthrough.md > '- **[llm_client.py](file:///d:/cody/2-2/llm_client.py)**:'
잘한 점
모듈별 역할(llm/place/report/config/main)로 기능을 분리해 구조를 명확히 설명함
부족한 점
전체 데이터 흐름(입력→검색→리포트)에 대한 간단한 다이어그램 보완 여지
보완
README나 문서에 간단한 아키텍처 다이어그램으로 모듈 간 데이터 흐름을 추가하세요
FAIL
평가 항목 #7
근거
llm_client.py > 'required_keys = ["recommended_city", "weather", "events", "reason"]'
잘한 점
필수 키 존재 여부를 체크하는 검증 로직을 구현한 점은 적절함
부족한 점
타입 검사(예: events가 배열인지)와 값 유효성 검증이 누락되어 있음
보완
각 필드의 기대 타입과 최소/최대 길이 등 구체적 검증 항목을 설계하고 구현하세요
PASS
평가 항목 #8
근거
place_client.py > 'if self.kakao_api_key:'
잘한 점
Kakao/Naver 호출을 한 클래스에 캡슐화하여 설정만으로 교체 가능하도록 설계했음
부족한 점
플러그인 패턴·인터페이스 명세가 문서로 명시되면 더 명확해짐
보완
지도 클라이언트 인터페이스(예: search(city)->list)를 명세해 교체 시 테스트를 간소화하세요
PASS
평가 항목 #9
근거
report_generator.py > '"errors": errors'
잘한 점
errors 리스트를 누적·저장하고 리포트에 반영하는 흐름이 구현되어 있음
부족한 점
에러 항목에 발생 시각/심각도 등의 메타가 없어서 분석에 제약이 있음
보완
각 에러 항목에 timestamp와 severity 필드를 추가하는 것을 고려하세요
PASS
평가 항목 #10
근거
README.md > 'GET 요청: Kakao/Naver 장소 검색 API 호출 시 쿼리 파라미터를 사용한 데이터 조회'
잘한 점
GET/POST 용도와 이유를 README에 명확히 설명함
부족한 점
특정 엔드포인트별 예시(헤더/쿼리 예시)가 더 있으면 이해가 쉬움
보완
요청 예시(간단한 cURL 또는 requests 예시)와 함께 메서드 선택 이유를 보강하세요
PASS
평가 항목 #11
근거
README.md > '비구조적인 LLM 텍스트 응답을 JSON 형식으로 강제 및 파싱하여'
잘한 점
LLM 출력의 JSON 강제를 문서와 프롬프트에서 명확히 설명하고 있음
부족한 점
JSON 강제 실패 시의 포맷 예시(잘못된 사례)가 문서화되어 있지 않음
보완
프롬프트/응답의 예시(정상/비정상)를 README에 추가하여 디버깅을 용이하게 하세요
PASS
평가 항목 #12
근거
place_client.py > 'Kakao API 인증 실패 (HTTP {response.status_code}). API 키를 확인하세요.'
잘한 점
401/403 처리 코드와 안내 메시지를 통해 원인(키/권한) 점검을 유도함
부족한 점
쿼터/네트워크 원인에 대한 재시도·백오프 전략은 구체화되어 있지 않음
보완
401/403 외 네트워크·쿼터 문제에 대해 재시도/백오프 정책을 문서화하세요
PASS
평가 항목 #
근거
README.md > '**보안 관리 정책**: API 키를 소스코드, README, Public Git 저장소에 절대로 노출하지 마세요.'
잘한 점
환경변수(.env) 사용과 보안 이점을 README에 분명히 기술함
부족한 점
운영환경(배포)에서의 키 교체/비밀관리(예: Vault) 연계 언급은 없음
보완
운영 전용 키 관리(환경별 비밀관리 툴 연계)를 간단히 권장 문구로 추가하세요
PASS
평가 항목 #14
근거
llm_client.py > 'for attempt in range(1, 3):'
잘한 점
LLM 1차 JSON 파싱 실패 시 최대 1회 재시도하고 프롬프트를 수정하는 전략을 구현함
부족한 점
재시도 후의 실패 기준과 후속 알림(운영자 통보 등)이 문서화되어 있지 않음
보완
재시도 실패 시 알림이나 메트릭 기록을 연동하는 방안을 고려하세요
PASS
평가 항목 #15
근거
llm_client.py > 'places_formatted = "- 데이터 없음 (장소 검색 결과 0건 또는 API 호출 불가)"'
잘한 점
맛집 0건 처리 시 '데이터 없음' 표기 및 리포트 진행 로직이 구현되어 있음
부족한 점
사용자에게 대체 제안(근접지역 재검색) 등 추가 UX 옵션은 없음
보완
0건일 때 대체 검색(근접 도시/유사 키워드) 옵션을 문서화하거나 플래그로 제공하세요
PASS
평가 항목 #16
근거
travel_planner.py > 'cached_data = report_generator.check_cache(target_date)'
잘한 점
동일 날짜 재실행 시 results 재사용으로 API 호출을 건너뛰는 캐시 기능을 구현함
부족한 점
캐시 만료 정책(언제 갱신할지)에 대한 명세가 없음
보완
캐시 유효기간 또는 강제 갱신 옵션을 문서화하세요
FAIL
평가 항목 #17
근거
place_client.py > 'query = f"{city} 맛집"'
잘한 점
실제 추천도시를 그대로 쿼리로 사용해 빠르게 검색을 수행함
부족한 점
입력 정규화(키워드 보정, 세부지역 추출, 재질문) 로직이 구현되어 있지 않음
보완
사용자 입력 정규화/보정(예: 별칭 매핑, 지역 분해, 추가 질의)을 설계·문서화하여 검색 정확도를 높이세요